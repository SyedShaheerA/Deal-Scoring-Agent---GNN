# Deal-Scoring-Agent---GNN
The Deal Scoring Agent uses a Graph Neural Network (GNN) to rank business listings. It builds a graph connecting "market comps" by industry and price. Using GraphSAGE, it learns 32-dim embeddings to score deals based on their structural fit against a buyer's specific profile.
